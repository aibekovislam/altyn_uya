import { Metadata } from "next";
import { ReduxProvider } from "../redux/provider";
import NavbarAsk from "@/components/Navbar/NavbarAsk";

export const metadata: Metadata = {
    title: "Алтын уя - анкета",
    description: "Generated by create next app",
};  

export default function Layout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
    return (
      <html lang="en">
        <link rel='canonical' href='https://www.qurban.kg/form/' />
        <body>
          <ReduxProvider>
            <NavbarAsk selectedSection="Анкета" />
            {children}
          </ReduxProvider>
        </body>
      </html>
    );
  }
  